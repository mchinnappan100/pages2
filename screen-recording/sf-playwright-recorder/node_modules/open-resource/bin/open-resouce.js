#!/usr/bin/env node

import { openResource } from "../src/index.js";

const resource = process.argv[2];

if (!resource) {
  console.log("Usage: open-resource <file-or-url>");
  process.exit(1);
}

openResource(resource);
