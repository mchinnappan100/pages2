
Now the model becomes:

* **User Story → 1 Feature Branch (recommended)**
* **Feature Branch → many Commits**
* **Promotion → many User Stories**
* **Promotion → indirectly includes Commits (via User Stories → Branch → Commits)**

---

## 🧭 Updated Mermaid ER Diagram

```mermaid
erDiagram

    PROMOTION ||--o{ USER_STORY : "includes (1:N)"
    USER_STORY ||--|| FEATURE_BRANCH : "uses (1:1 recommended)"
    FEATURE_BRANCH ||--o{ COMMIT : "contains (1:N)"

    PROMOTION {
        string id
        string environment
        datetime promoted_at
    }

    USER_STORY {
        string id
        string name
        string status
    }

    FEATURE_BRANCH {
        string name
        string repo
    }

    COMMIT {
        string hash
        string author
        datetime timestamp
    }
```

---

## 🔄 Visual Flow (End-to-End DevOps View)

```mermaid
flowchart LR

    PR[Promotion] -->|includes| US1[User Story A]
    PR -->|includes| US2[User Story B]

    US1 --> FB1[Feature Branch A]
    US2 --> FB2[Feature Branch B]

    FB1 --> C1[Commit 1]
    FB1 --> C2[Commit 2]

    FB2 --> C3[Commit 3]
    FB2 --> C4[Commit 4]
```

---

## 🎯 Key Insight

What actually gets deployed in Copado is:

> ✅ **Commits associated with User Stories (not the branch itself)**

So your full chain is:

```
Promotion → User Stories → Feature Branch → Commits → Metadata deployed
```

---

